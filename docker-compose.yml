services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: harmonista
      POSTGRES_PASSWORD: harmonista
      POSTGRES_DB: harmonista
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data2

  valkey:
    image: valkey/valkey:8-alpine
    restart: unless-stopped
    ports:
      - "6378:6379"
    volumes:
      - valkeydata:/data

volumes:
  pgdata:
  valkeydata:
